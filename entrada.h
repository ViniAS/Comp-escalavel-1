#include <iostream>
#include <fstream>
#include <vector>
#include <string>
using namespace std;
#ifndef ENTRADA_H
#define ENTRADA_H

vector<string> lerArquivo(string nomeArquivo);
#endif //UNTITLED_ENTRADA_H
